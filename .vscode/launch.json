{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug LazyMail (TUI)",
            "type": "go",
            "request": "launch",
            "mode": "debug",
            "program": "${workspaceFolder}/cmd/lazymail",
            "cwd": "${workspaceFolder}",
            "console": "integratedTerminal",
            "env": {
                "GOPROXY": "https://goproxy.cn,direct"
            }
        }
    ]
}